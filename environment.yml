# conda env create -f environment.yml -n idr-omero
name: torch
channels:
  - idr
  - torch
  - ome
  - defaults
  - bioconda
dependencies:
  - idr-py
  - torchvision
  - tensorboard
  - pytorch
  - matplotlib
  - pip 
  - pip:
    - nonechucks